<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WTGWizard.Pages.WelcomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WTGWizard.Pages"
    xmlns:lang="using:WTGWizard.Main.Language"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="12">
            <TextBlock Text="{x:Bind lang:Lang.Page_Welcome_WelcomeTitle}"
                       Style="{ThemeResource TitleLargeTextBlockStyle}"
                       HorizontalAlignment="Center" />
            <TextBlock Text="{x:Bind lang:Lang.Page_Welcome_WelcomeMsg}"
                       Style="{ThemeResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center"
                       TextWrapping="Wrap" />
            <Button Content="{x:Bind lang:Lang.Page_Welcome_GoWizard_Content}"
                    Style="{ThemeResource AccentButtonStyle}"
                    HorizontalAlignment="Center"
                    Click="GoWizard_Button_Click"
                    Margin="0,16,0,0" />
        </StackPanel>
  </Grid>
</Page>
